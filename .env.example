# HomeConnect API Credentials
# Erhalte diese von https://developer.home-connect.com/applications
HOME_CONNECT_CLIENT_ID=deine-client-id-hier
HOME_CONNECT_CLIENT_SECRET=dein-client-secret-hier

# OAuth Redirect URI
# Muss exakt mit der in der Application-Registrierung übereinstimmen
HOME_CONNECT_REDIRECT_URI=http://localhost:3000/callback

# Home Appliance ID (HAID)
# Lässt du zunächst leer - diese findest du nach dem ersten Auth-Flow heraus
# Mit `make status` oder `python -m scripts.device_status` alle registrierten Geräte anzeigen
# Die HAID steht im JSON-Output unter `data.homeappliances[].haid`
HOME_CONNECT_HAID=

# Scopes für die HomeConnect API
# Werden beim Auth-Flow angefordert (nicht bei der Application-Registrierung)
HOME_CONNECT_SCOPE=IdentifyAppliance Control CoffeeMaker Settings Monitor

# Optional: Pfad zur Token-Datei (Standard: tokens.json)
# HOME_CONNECT_TOKEN_PATH=tokens.json

# Optional: API Token für HTTP-Server Authentifizierung
# COFFEE_API_TOKEN=mein-token

# Optional: Hostname für SSL-Zertifikat (z.B. dein-hostname.local)
# Wird beim Erstellen des Zertifikats verwendet: make cert
# Falls gesetzt, wird dieser Hostname zusätzlich zu localhost und *.local zum Zertifikat hinzugefügt
# CERT_HOSTNAME=dein-hostname.local
